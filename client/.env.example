# Copy this file to .env and fill in real values.
# Only needed if the API is hosted on a different origin than the client
# (in local dev, Vite's proxy in vite.config.js handles this automatically).
VITE_API_URL=
